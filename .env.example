# ===========================================
# PostgreSQL Database Configuration
# ===========================================
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_strong_password_here
PGDATA=/var/lib/postgresql/data

# ===========================================
# Application Ports Configuration
# ===========================================
BACKEND_PORT=4000
FRONTEND_PORT=9000
ADMINER_PORT=9001

# ===========================================
# Docker Configuration
# ===========================================
# Docker registry (Docker Hub username or GitHub Container Registry)
DOCKER_REGISTRY=maxorel

# Image tag for deployment (auto-set by CI/CD)
IMAGE_TAG=latest

# ===========================================
# Application Configuration
# ===========================================
# Node environment (development, staging, production)
NODE_ENV=development

# TypeORM auto-sync (1 = enabled, 0 = disabled)
# WARNING: Set to 0 in production!
TYPEORM_SYNC=1

# ===========================================
# Frontend API URL Configuration
# ===========================================
# API URL for frontend to connect to backend
# Examples:
# - Local: http://localhost:4000
# - Staging: https://api-staging.yourdomain.com
# - Production: https://api.yourdomain.com
REACT_APP_API_URL=http://localhost:4000/

# ===========================================
# Deployment Configuration (for CI/CD)
# ===========================================
# These are used by deployment scripts
DEPLOY_DIR=/home/deploy